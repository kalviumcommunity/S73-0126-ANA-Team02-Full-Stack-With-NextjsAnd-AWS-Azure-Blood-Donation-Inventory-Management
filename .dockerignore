# Exclude dependencies and build outputs from the Docker context
node_modules
.next

# Exclude environment files (avoid baking secrets into images)
.env
.env.*

# Exclude Git metadata and config
.git
.gitignore
.github

# Common log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
