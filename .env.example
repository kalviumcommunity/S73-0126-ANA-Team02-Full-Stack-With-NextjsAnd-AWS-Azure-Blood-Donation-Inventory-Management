# Example environment variables for this Next.js App Router project.
# Copy this file to `.env.local` and replace placeholder values with real secrets.
# Do NOT commit `.env.local`.

# --------------------
# Database Configuration
# --------------------
# PostgreSQL connection string for Prisma
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA
# Local example: postgresql://postgres:postgres@localhost:5432/blood_bank_db?schema=public
# Cloud example: postgresql://user:pass@db.example.com:5432/dbname?schema=public
DATABASE_URL="postgresql://postgres:password@localhost:5432/blood_bank_db?schema=public"

# --------------------
# Server-side variables (do NOT expose to client)
# --------------------
# Database connection string (Postgres example):
# DATABASE_URL=postgres://username:password@localhost:5432/database_name
# DATABASE_URL=postgres://<username>:<password>@<host>:5432/<database>

# Redis connection string (used for caching, sessions):
# REDIS_URL=redis://:password@redis-host:6379
REDIS_URL=redis://<password>@<host>:6379

# Example secret used only on the server (e.g., Stripe):
# STRIPE_SECRET_KEY=sk_test_...
STRIPE_SECRET_KEY=<your_server_secret>

# --------------------
# Client-side variables (safe to expose in browser)
# --------------------
# Public API base URL (available on both server and client)
# Must be prefixed with NEXT_PUBLIC_
# NEXT_PUBLIC_API_BASE_URL=https://api.example.com
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000/api

# Notes:
# - Variables without NEXT_PUBLIC_ are only accessible server-side in Next.js.
# - After changing environment variables, restart the Next.js dev server.
# - Never commit real secrets to the repository.
